name: Controlled Release

on:
	pull_requests:
		branches:
			- main

jobs:
	build:
		runs-on: windows-latest
		steps:
			- name: Checkout repository
			uses: actions/checkout@v4

			- name: Run a simple script
			run: echo "Pull request detected! Running worflow..."